# 角色属性显示插件 - 修改日志

## 版本 2.1.0 - 属性变化颜色提示

### 新增功能

#### 1. 属性变化颜色提示
- **功能描述**: 当属性数值提升时，数值颜色变为**绿色**
- **提升时**: 数值显示为**绿色**（如：暴击从 17.7% → 37.7%）
- **非提升状态**: 数值恢复为默认颜色
- **实现方式**: 基于数值比较，智能判断提升状态

#### 2. 技术改进
- 新增 `statChangeDirection` 缓存，记录每个属性的提升状态
- 新增 `ExtractNumber()` 函数，支持从百分比字符串提取数字
- 优化 `UpdateStat()` 函数，支持颜色变化逻辑
- 在重建文本时自动清空相关缓存

### 版本历史总结

| 版本 | 发布日期 | 主要更新 |
|------|----------|----------|
| 2.1.0 | 2026-03-15 | 属性变化颜色提示（提升时变绿色）|
| 2.0.0 | 2026-03-15 | 界面重构、主属性显示、设置界面、额外属性选项 |

---

## 版本 2.0.0 - 重大更新（界面重构与功能增强）

### 新增功能

#### 1. 主属性显示
- **功能描述**: 在装等下方新增主属性（力量/敏捷/智力）显示
- **智能识别**: 根据职业自动判断主属性类型
  - 力量职业: 战士、圣骑士、死亡骑士
  - 敏捷职业: 猎人、潜行者、恶魔猎手、武僧
  - 智力职业: 法师、牧师、术士
  - 混合职业: 德鲁伊、萨满（根据专精自动切换）
  - 唤魔师: 智力
- **显示格式**: `力量: 1297` / `敏捷: 1520` / `智力: 3850`

#### 2. 绿字排版顺序调整
- **调整前**: 急速 → 暴击 → 精通 → 全能
- **调整后**: 暴击 → 急速 → 精通 → 全能
- **调整原因**: 与魔兽世界默认角色面板顺序保持一致，提升用户习惯一致性

#### 3. 界面布局优化
- **文字对齐**: 所有文字改为左对齐
- **框体尺寸**: 宽度固定160px，高度根据显示属性数量动态计算
- **内边距优化**: 统一8px内边距，紧凑但保留适当冗余
- **行高调整**: 16px行高，确保文字不拥挤

#### 4. 设置界面
- **打开方式**: 输入 `/csd` 或 `/csd config` 或 `/csd settings`
- **功能模块**:
  - **框体移动控制**: 锁定/解锁框体拖动功能
  - **额外属性选项**: 吸血、招架、闪避、格挡的显示开关
  - **位置重置**: 一键重置到默认位置

#### 5. 额外可选属性
- **吸血**: 生命偷取百分比（绿色）
- **招架**: 招架几率（白色）
- **闪避**: 闪避几率（白色）
- **格挡**: 格挡几率（白色）
- **默认状态**: 所有可选属性默认隐藏
- **动态框体**: 根据勾选的属性自动调整框体高度

#### 6. 数据持久化
- **位置保存**: 框体位置自动保存，下次登录恢复
- **设置保存**: 锁定状态和属性显示选项自动保存
- **存储变量**: `CharacterStatsDisplayDB`

### 界面变化对比

#### 旧版界面 (v1.x)
```
┌──────────────────┐
│     角色属性     │
│ 装等: 226.8      │
│ 急速: 13.0%      │  ← 第一个绿字
│ 暴击: 17.7%      │
│ 精通: 16.6%      │
│ 全能: 11.8%      │
│ 移速: 100%       │
└──────────────────┘
```

#### 新版界面 (v2.0.0)
```
┌──────────────────┐
│ 角色属性         │  ← 左对齐
│ 装等: 226.8      │
│ 力量: 1297       │  ← 新增主属性
│ 暴击: 17.7%      │  ← 绿字顺序调整
│ 急速: 13.0%      │
│ 精通: 16.6%      │
│ 全能: 11.8%      │
│ [吸血: 3.0%]     │  ← 可选属性（默认隐藏）
│ [招架: 15.0%]    │
│ [闪避: 3.0%]     │
│ [格挡: 24.0%]    │
│ 移速: 100%       │
└──────────────────┘
```

### 技术改进

#### 1. 代码重构
- 引入配置数据库系统
- 属性文本动态创建机制
- 框体尺寸动态计算

#### 2. 事件扩展
- 新增 `ACTIVE_TALENT_GROUP_CHANGED` 监听
- 新增 `PLAYER_SPECIALIZATION_CHANGED` 监听
- 主属性在天赋/专精切换时自动更新

#### 3. API扩展
- 新增 `GetPlayerPrimaryStat()` 主属性获取
- 新增 `GetPlayerLeech()` 吸血获取
- 新增 `GetPlayerParry()` 招架获取
- 新增 `GetPlayerDodge()` 闪避获取
- 新增 `GetPlayerBlock()` 格挡获取

### 命令变更

| 命令 | v1.x 功能 | v2.0.0 功能 |
|------|-----------|-------------|
| `/csd` | 显示帮助 | **打开设置界面** |
| `/csd config` | 无 | 打开设置界面 |
| `/csd settings` | 无 | 打开设置界面 |
| `/csd show` | 显示面板 | 显示面板 |
| `/csd hide` | 隐藏面板 | 隐藏面板 |
| `/csd reset` | 重置位置 | 重置位置 |
| `/csd update` | 手动更新 | 手动更新 |

---

## 版本 1.0.3 - 战斗状态感知更新（绿字属性高频支持）

### 优化目标
解决战斗中buff频繁改变绿字属性时的实时显示需求

### 核心问题
- **非战斗状态**: 绿字属性稳定，低频更新即可
- **战斗状态**: 触发类buff、饰品效果、技能增益等会频繁改变绿字属性，需要高频更新才能准确反映

### 优化方案：战斗状态感知系统

#### 1. 动态频率调整
根据战斗状态自动切换更新频率：

| 状态 | 绿字属性更新周期 | 原因 |
|------|------------------|------|
| **战斗中** | **0.5秒** | buff频繁触发，需要实时反映属性变化 |
| **非战斗** | 3秒 | 属性稳定，低频更新节省资源 |

#### 2. 战斗事件监听
```lua
-- 进入战斗
PLAYER_REGEN_DISABLED → 启动高频更新(0.5秒)

-- 脱离战斗  
PLAYER_REGEN_ENABLED → 恢复低频更新(3秒)
```

#### 3. 智能定时器管理
```lua
local function StartCombatUpdates()
    combatUpdateTicker = C_Timer.NewTicker(0.5, function()
        UpdateGreenStats()  -- 绿字属性
        UpdateSpeed()       -- 移速
    end)
end

local function StopCombatUpdates()
    if combatUpdateTicker then
        combatUpdateTicker:Cancel()
    end
end
```

#### 4. 分层更新架构
```lua
-- 移速：始终0.5秒（与战斗无关）
C_Timer.NewTicker(0.5, UpdateSpeed)

-- 绿字属性：根据战斗状态动态调整
-- 战斗中：0.5秒，非战斗：3秒

-- 装等：始终3秒（变化频率极低）
```

### 性能优化策略

1. **脏检查机制**: 属性值未变化时不重绘UI
2. **按需启动**: 只在战斗中启动高频定时器
3. **及时释放**: 脱离战斗后立即停止高频定时器
4. **节流控制**: 每次更新间隔强制限制，防止过度调用

### 更新策略对比

| 场景 | 版本1.0.2 | 版本1.0.3 |
|------|-----------|-----------|
| 非战斗绿字更新 | 3秒 | 3秒 |
| **战斗绿字更新** | **3秒** | **0.5秒** ✓ |
| 移速更新 | 0.5秒 | 0.5秒 |
| 战斗中CPU占用 | 低 | 中等（仅战斗中） |
| 非战斗CPU占用 | 低 | 极低 |

### 技术亮点

1. **状态感知**: 自动识别战斗/非战斗状态
2. **动态调度**: 根据场景自动调整更新策略
3. **资源优化**: 高频更新仅在需要时启用
4. **无缝切换**: 战斗开始/结束瞬间切换频率

---

## 版本 1.0.2 - 性能优化（高频更新支持）

### 优化目标
在保持游戏流畅的前提下，实现1秒以内的高频属性更新

### 优化方案

#### 1. 属性分级更新策略
不同属性的变化频率不同，采用差异化的更新周期：

| 属性类型 | 更新周期 | 原因 |
|----------|----------|------|
| 移速 | 0.5秒 | 移动时实时变化，需要高频更新 |
| 装等/急速/暴击/精通/全能 | 3秒 | 变化频率低，低频更新即可 |

#### 2. 脏检查机制（Dirty Check）
引入属性缓存系统，只在属性值真正变化时才更新UI：
```lua
local currentStats = {
    itemLevel = "",
    haste = "",
    crit = "",
    mastery = "",
    versatility = "",
    speed = ""
}

local function UpdateStat(key, value, index)
    if currentStats[key] ~= value then  -- 脏检查
        currentStats[key] = value
        CharacterStatsDisplay.stats[key]:SetText(...)
    end
end
```

**优势**：
- 避免无意义的UI重绘
- 减少CPU和GPU负载
- 即使检查频率高，实际更新次数很少

#### 3. 双定时器架构
分离不同频率的更新任务：
```lua
-- 高频：移速更新（0.5秒）
C_Timer.NewTicker(0.5, UpdateSpeed)

-- 低频：其他属性更新（3秒）
C_Timer.NewTicker(3, UpdateSlowStats)
```

### 性能对比

| 指标 | 版本1.0.0 | 版本1.0.1 | 版本1.0.2 |
|------|-----------|-----------|-----------|
| 更新周期 | 1秒（全部） | 3秒（全部） | 0.5秒（移速）+ 3秒（其他） |
| 实际UI更新次数 | 每秒6次 | 每3秒最多6次 | 视变化情况而定 |
| 卡顿情况 | 严重卡顿 | 无卡顿 | 无卡顿 |
| 移速响应性 | 实时 | 延迟3秒 | 延迟0.5秒 |

### 技术亮点

1. **智能节流**: 高频检查 + 低频实际更新
2. **按需渲染**: 只有数据变化才重绘UI
3. **资源隔离**: 移速单独更新，不影响其他属性

---

## 版本 1.0.1 - Bug修复

### 修复的问题

#### 1. 属性显示问题 [已修复]
**问题描述**: 除装等外，其他属性（急速、暴击、精通、全能、移速）都显示为"--"

**根本原因**: 函数名与魔兽世界API函数名冲突
- 原代码中定义了 `GetHaste()` 函数，但函数内部调用 `GetHaste()` 时，Lua会优先使用局部变量而非全局API
- 同样的冲突存在于 `GetCrit()` 等函数

**修复方案**: 将所有获取属性的函数重命名，添加"Player"前缀
- `GetHaste()` → `GetPlayerHaste()`
- `GetCrit()` → `GetPlayerCrit()`
- `GetMastery()` → `GetPlayerMastery()`
- `GetVersatility()` → `GetPlayerVersatility()`
- `GetSpeed()` → `GetPlayerSpeed()`
- `GetItemLevel()` → `GetPlayerItemLevel()`

#### 2. 游戏卡顿问题 [已修复]
**问题描述**: 安装插件后游戏画面一秒一卡

**根本原因**:
1. 使用了 `C_Timer.NewTicker(1, UpdateStats)` 每秒更新一次，频率过高
2. 注册了 `UNIT_STATS` 和 `UNIT_AURA` 事件，这两个事件触发极其频繁（每秒可能触发数十次）
3. 每次事件触发都会调用 `UpdateStats()`，造成大量不必要的计算

**修复方案**:
1. **降低定时器频率**: 将 `C_Timer.NewTicker(1, UpdateStats)` 改为 `C_Timer.NewTicker(3, UpdateStats)`，每3秒更新一次
2. **移除高频事件**: 取消注册 `UNIT_STATS` 和 `UNIT_AURA` 事件
3. **添加节流机制**: 在 `UpdateStats()` 函数中添加2秒冷却时间，防止短时间内重复更新
4. **保留必要事件**: 只保留 `PLAYER_LOGIN`（登录时初始化）和 `PLAYER_EQUIPMENT_CHANGED`（装备更换时更新）

### 新增功能
- 添加 `/csd update` 命令，允许玩家手动刷新属性显示

### 文件变更
- `Core.lua` - 修复属性获取函数名冲突，优化更新机制

---

## 版本历史总结

| 版本 | 发布日期 | 主要更新 |
|------|----------|----------|
| 2.0.0 | 2026-03-15 | 界面重构、主属性显示、设置界面、额外属性选项 |
| 1.0.3 | - | 战斗状态感知、动态更新频率 |
| 1.0.2 | - | 性能优化、脏检查机制、双定时器架构 |
| 1.0.1 | - | Bug修复、函数名冲突解决、卡顿优化 |
| 1.0.0 | - | 初始版本 |

---

*文档版本: 2.0.0*
*最后更新: 2026-03-15*
